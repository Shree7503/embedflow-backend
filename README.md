## 🔧 Setup Instructions

### 1. Clone the Repository

```
git clone https://github.com/your-username/embedflow.git
cd embedflow
```

### 2. Run Docker for nginx reverse proxy

```
docker-compose up --build
```
Note: NGINX runs on default port 8000 and reverse proxy's services to port: 300x

